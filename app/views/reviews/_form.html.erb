<%= form_for [@product, @review], remote: true do |f| %>
  <div class="field">
    <%= f.label :review %><br />
    <%= f.text_area :review, required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
